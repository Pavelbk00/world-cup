## КРИТИЧЕСКИЙ СБРОС И ВЫПОЛНЕНИЕ ЗАДАЧИ ПО БОМБАРДИРАМ

1. **Забудь прошлый план:** Прекрати повторно вызывать одни и те же упавшие инструменты.
2. **Логика триггера финала:**
   - Измени скрипт, который вызывается через `npm run fetch-results`.
   - Добавь проверку: если матч `wc-104` завершен, сделай GET-запрос к API для получения списка бомбардиров.
   - Если у "Lionel Messi" > 10 голов, запиши в `/var/www/world-cup/data/tournament-results.json` в поле `goldenBootWinner` значение "Лионель Месси".
   - Иначе запиши/оставь "Килиан Мбаппе".
3. **План отладки для ИИ:** Если внутренние инструменты чтения/записи файлов Cline выдают ошибку, выполни эту задачу, написав изолированный проверочный скрипт `test-patch.js` и запустив его напрямую через команду терминала `node test-patch.js`.
